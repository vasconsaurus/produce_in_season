<section class="navbar">
  <div class="logo">
    <%= I18n.t('root.title') %>
  </div>
  <div class="nav-right">
    <p>procure pelo</p>
    <%= link_to I18n.t('action.month').capitalize, item_seasons_path, class: 'link_action' %>
    <%= link_to I18n.t('action.produce').capitalize, item_seasons_path, class: 'link_action' %>
  </div>
</section>
