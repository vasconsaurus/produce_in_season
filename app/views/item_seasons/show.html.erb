<div class="content background-color-blue">
  <h1 class="page__title banner-small m-bottom-m"> <%= formatted_month_name(@item_season.month_index).capitalize %> </h1>

  <div class="nav-actions-holder p-sides-m">
    <h4><%= link_to I18n.t("action.back").upcase, item_seasons_path, class: "link_action back" %></h4>
    <div class="nav-actions-months">
      <%= link_to item_season_path(month_navigation_previous(@item_season)) do %>
        <i class="fa-solid fa-arrow-left-long" title="arrow-navigate-left" aria-hidden="true" aria-label="arrow-navigate-left"></i>
      <% end %>
      <h4 class="color-yellow p-sides-s"><%= I18n.t("action.month") %></h4>
      <%= link_to item_season_path(month_navigation_next(@item_season)) do %>
        <i class="fa-solid fa-arrow-right-long" title="arrow-navigate-right" aria-hidden="true" aria-label="arrow-navigate-right"></i>
      <% end %>
    </div>
  </div>

  <div class="card-holder p-top-l p-bottom-l p-sides-s">
    <% @item_season_produces.find_each do |produce| %>
      <div class="card">
        <div class="card-inner">
          <h4 class="produce-name"><%= link_to I18n.t("produce_items.names.#{produce.name}"), produce, class: "link_action"  %></h4>
        </div>
          <div class="produce-category"><%= I18n.t("produce_items.categories.#{produce.category}") %></div>
      </div>
    <% end %>
  </div>

</div>
