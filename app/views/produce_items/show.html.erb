<p id="notice"><%= notice %></p>

<h1> <%= @produce_item.name_with_whitespace %> </h1>
<h3> months </h3>

<% @produce_item.item_seasons.each do |season|  %>
  <p><%= format_month_name(season.month_index)%></p>
<% end %>

<%= link_to 'Back', produce_items_path %>
